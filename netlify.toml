# Netlify deploy configuration for Goldsmith Casework Co.
# Git-connected: Netlify runs this build on every push to the production branch.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Friendly 404 handling is provided by Astro's 404.astro page.
