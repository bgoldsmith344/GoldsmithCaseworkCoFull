---
import { link } from '../lib/links.ts';
interface Props {
  href: string;
  variant?: 'primary' | 'secondary';
  class?: string;
}
const { href, variant = 'primary', class: className = '' } = Astro.props;
---
<a href={link(href)} class={`btn btn--${variant} ${className}`}>
  <slot />
</a>
